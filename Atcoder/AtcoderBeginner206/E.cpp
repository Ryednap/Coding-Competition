#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL
#include "uj.h"
#endif

const int nax = 1e6 + 4;

int main() {
	int l, r;
	scanf("%d %d", &l, &r);

	int arr[nax];
	for( int i = l; i <= r; ++i ) {
		arr[i] = (r - l);
	}

	


	return 0;
}
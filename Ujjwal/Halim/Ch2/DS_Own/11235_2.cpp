#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL
#include "uj.h"
#endif

using ui = unordered_map < int, int >;

int main() {
	int n;
	while ( scanf("%d", &n) && n ) {
		int qq; 
		scanf("%d", &qq);
		
		ui first, last, cnt;
		int freq[n], arr[n];
		for(int i = 0; i < n; ++i) {
			scanf("%d", &arr[i]);
		}
		
		
	
	return 0;
}

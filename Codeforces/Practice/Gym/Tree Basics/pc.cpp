#include <bits/stdc++.h>
using namespace std;

#ifdef LOCAL
#include "uj.h"
#endif
//debug & operator << (debug & , const ob & o) {
	//d << "( " << o. << " " << o. " )";
	//return d;
//}


int main() {
	int qq;
	scanf("%d", &qq);
	while(qq--) {
		int n;
		scanf("%d",&n);
		int arr[n];
		for(int i = 0; i < n; ++i) {
			scanf("%d", &arr[i]);
		}
		int dp[2] = 0;
		for(int i = 0; 
		
		printf("%d\n", min(dp[0], dp[1]));
			
	}
	return 0;
}
